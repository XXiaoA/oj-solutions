#include <cstdio>
using namespace std;

int main() {
    // 审题，单精度浮点
    float n;
    // cin >> a;
    // cout << fixed << setprecision(3) << a;
    scanf("%f", &n);
    printf("%.3f", n);

    return 0;
}
